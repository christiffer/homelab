# Handlers for common role

---
- name: Restart SSH
  service:
    name: ssh
    state: restarted

- name: Reboot required
  debug:
    msg: "Reboot required for changes to take effect. Run 'sudo reboot' when ready."
